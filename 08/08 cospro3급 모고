1번:
a = input()
d = 0 #대문자수
s = 0 #소문자수
h = 0 

for i in a:
    if i.isupper():
        d += 1
    elif i.islower():
        s += 1
h = abs(d - s)
print(h)

2번:
a = input().split()
b = []

for i in range(len(a)):
    if len(a[i]) >= 5:
        b.append(a[i])
b.sort
print(b)

3번:
a = list(map(int, input().split()))
b = 0
p = 0
s = 0

for i in range(len(a)):
    b += a[i]
    p = b / (i + 1)
    if a[i] > p:
        s += 1
print(s)

4번:
numbers = list(map(int, input().split()))

max_sum = numbers[0] + numbers[1]

for i in range(len(numbers)-1):
    if max_sum < numbers[i] + numbers[i + 1]:
        max_sum == numbers[i] + numbers[i + 1]
print(max_sum)

5번:
a = int(input())
b = 0

if a > 3:
    b = 500 * 3 + (a - 3) * 1000

elif a <= 3:
    b = 500 * a
print(b)
   
6번:
participants = ['Kim', 'Park', 'Lee', 'Choi', 'Jung', 'Kang']
print(participants[-3:])

7번:
def solution(arr):
    max_val = arr[0]
    max_idx = 0
    for i in range(1, len(arr)):
        if arr[i] > max_val:
            max_val = arr[i]
            max_idx = i
    return max_idx
8번:
def solution(word):
    vowels = "aeiou"
    answer = ""
    for char in word:
        if char not in vowels:
            answer += char
    return answer

9번:
def solution(list1, list2):
    answer = []
    for i in range(len(list1)):
        answer.append(list1[i] + list2[i])
    return answer

10번:
def solution(num_str):
    total = 0
    for char in num_str:
        total += int(char)
    return total


